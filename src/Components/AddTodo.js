import React, { Component } from "react";

class AddTodo extends Component {
  render() {
    return (
      <div>
        <input type="text" />
        <button>Add</button>
      </div>
    );
  }
}

export default AddTodo;
